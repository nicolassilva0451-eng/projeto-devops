name: CI
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Alerta sucesso
        if: ${{ success() }}
        run: echo "Pipeline OK"
      - name: Alerta falha
        if: ${{ failure() }}
        run: echo "Falha"
